{
  "_comment": "Fallback for Devin Desktop when it loads the legacy Cascade hook format instead of .devin/hooks.v1.json. Same scripts, different event names and contract (exit code 2 blocks; stdout is ignored). Harmless if Desktop actually reads .devin/hooks.v1.json — then this file is simply never loaded. Check with /hooks, or by running a blocked command and seeing whether it gets stopped.",

  "pre_run_command": [
    {
      "command": "./scripts/guard-prod.sh",
      "working_directory": ".",
      "show_output": true
    }
  ],

  "post_write_code": [
    {
      "command": "./scripts/redaction-scan.sh 2>&1 | grep -E '^\\[FAIL\\]' || true",
      "working_directory": ".",
      "show_output": true
    }
  ],

  "post_cascade_response": [
    {
      "command": "./scripts/stop-gate.sh",
      "working_directory": ".",
      "show_output": true
    }
  ]
}
